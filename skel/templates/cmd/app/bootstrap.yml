info.app:
  name: ${app.name}
  description: ${app.description}
  attributes:
    displayName: ${app.displayName}
    parent: platform
    type: platform

server:
  port: ${server.port}
  contextPath: ${server.contextPath}
  staticPath: ../../static
